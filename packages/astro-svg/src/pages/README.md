# Pages Directory

This directory exists to satisfy Astro's requirement for a `pages` directory during type checking, and
supresses the warning: "Missing pages directory."

Since this is a library package (not a full Astro application), we don't actually use routing or pages. The directory is intentionally kept empty.

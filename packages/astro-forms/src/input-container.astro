---
// packages/astro-forms/src/input-container.astro
import type { HTMLAttributes } from 'astro/types';
import './forms.css';

type Props = HTMLAttributes<'div'> & {};

const { class: className, ...rest } = Astro.props;
---

<div class:list={['form-input-container', className]} {...rest}>
  <slot />
</div>
